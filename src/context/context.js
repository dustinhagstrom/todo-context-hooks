import React from "react";

//can export as many contexts as I want.
//we can only export default once
export const TodoInputContext = React.createContext({});
export const TodoContext = React.createContext({});
